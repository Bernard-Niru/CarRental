@model CarRental.DTOs.CarDTO

@{
    ViewData["Title"] = "Update Car";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<!-- Page Title -->
<h2 class="mb-4 text-center text-gradient fw-bold">
    <i class="fas fa-edit me-2"></i> Update Car
</h2>

<!-- Glassmorphism Card -->
<div class="card glass-card shadow-lg mx-auto" style="max-width: 700px;">
    <div class="card-body p-4">
        <form asp-action="UpdateCar" asp-controller="Admin" method="post">

            <!-- Hidden Car ID -->
            <input type="hidden" asp-for="CarID" />

            <!-- Car Name -->
            <div class="form-group mb-3">
                <label asp-for="CarName" class="form-label fw-semibold">Car Name</label>
                <input asp-for="CarName" class="form-control stylish-input" placeholder="Enter car name..." />
                <span asp-validation-for="CarName" class="text-danger small"></span>
            </div>

            <!-- Brand -->
            <div class="form-group mb-3">
                <label asp-for="BrandID" class="form-label fw-semibold">Brand</label>
                <select asp-for="BrandID" class="form-select stylish-input" asp-items="ViewBag.BrandList">
                    <option value="">-- Select Brand --</option>
                </select>
                <span asp-validation-for="BrandID" class="text-danger small"></span>
            </div>

            <!-- Car Type -->
            <div class="form-group mb-3">
                <label asp-for="CarType" class="form-label fw-semibold">Car Type</label>
                <select asp-for="CarType" class="form-select stylish-input" asp-items="ViewBag.CarType">
                    <option value="">-- Select Car Type --</option>
                </select>
                <span asp-validation-for="CarType" class="text-danger small"></span>
            </div>

            <!-- Fuel Type -->
            <div class="form-group mb-3">
                <label asp-for="FuelType" class="form-label fw-semibold">Fuel Type</label>
                <select asp-for="FuelType" class="form-select stylish-input" asp-items="ViewBag.FuelType">
                    <option value="">-- Select Fuel Type --</option>
                </select>
                <span asp-validation-for="FuelType" class="text-danger small"></span>
            </div>

            <!-- Gear Type -->
            <div class="form-group mb-3">
                <label asp-for="GearType" class="form-label fw-semibold">Gear Type</label>
                <select asp-for="GearType" class="form-select stylish-input" asp-items="ViewBag.GearType">
                    <option value="">-- Select Gear Type --</option>
                </select>
                <span asp-validation-for="GearType" class="text-danger small"></span>
            </div>

            <!-- Color -->
            <div class="form-group mb-3">
                <label asp-for="Color" class="form-label fw-semibold">Color</label>
                <select asp-for="Color" class="form-select stylish-input" id="color-select">
                    <option value="">-- Select Color --</option>
                    <option value="White" style="background-color: white; color: black;">White</option>
                    <option value="Black" style="background-color: black; color: white;">Black</option>
                    <option value="Red" style="background-color: red; color: white;">Red</option>
                    <option value="Blue" style="background-color: blue; color: white;">Blue</option>
                    <option value="Silver" style="background-color: silver; color: black;">Silver</option>
                    <option value="Gray" style="background-color: gray; color: white;">Gray</option>
                    <option value="Green" style="background-color: green; color: white;">Green</option>
                    <option value="Other">Other</option>
                </select>
                <input type="text" id="custom-color-input" class="form-control mt-2 stylish-input" placeholder="Enter new color" style="display: none;" />
                <span asp-validation-for="Color" class="text-danger small"></span>
            </div>

            <!-- Seats -->
            <div class="form-group mb-3">
                <label asp-for="No_of_Seats" class="form-label fw-semibold">No. of Seats</label>
                <input asp-for="No_of_Seats" class="form-control stylish-input" placeholder="e.g., 5" />
                <span asp-validation-for="No_of_Seats" class="text-danger small"></span>
            </div>

            <!-- Rental Rate -->
            <div class="form-group mb-4">
                <label asp-for="RentalRate" class="form-label fw-semibold">Rental Rate</label>
                <input asp-for="RentalRate" class="form-control stylish-input" placeholder="e.g., 50 per day" />
                <span asp-validation-for="RentalRate" class="text-danger small"></span>
            </div>

            <!-- Submit Button -->
            <button type="submit" class="btn btn-gradient w-100 fw-bold">
                <i class="fas fa-save me-1"></i> Update Car
            </button>
        </form>
    </div>
</div>

<style>
    .glass-card {
        background: rgba(255, 255, 255, 0.15);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 1rem;
    }

    .text-gradient {
        background: linear-gradient(to right, #007bff, #6610f2);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .stylish-input {
        border-radius: 0.5rem;
        transition: box-shadow 0.3s, transform 0.2s;
    }

        .stylish-input:focus {
            box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
            transform: scale(1.02);
        }

    .btn-gradient {
        background: linear-gradient(135deg, #007bff, #6610f2);
        color: white;
        border: none;
        padding: 0.75rem;
        border-radius: 0.5rem;
        transition: background 0.3s, transform 0.2s;
    }

        .btn-gradient:hover {
            background: linear-gradient(135deg, #0056b3, #520dc2);
            transform: scale(1.03);
        }

    .fw-semibold {
        font-weight: 600;
    }
</style>

    <partial name="_ValidationScriptsPartial" />
<script>
    const colorSelect = document.getElementById("color-select");
    const customColorInput = document.getElementById("custom-color-input");

    colorSelect.addEventListener("change", function () {
        if (this.value === "Other") {
            customColorInput.style.display = "block";
        } else {
            customColorInput.style.display = "none";
        }
    });

    // Ensure the form uses custom color if selected
    document.querySelector("form").addEventListener("submit", function (e) {
        if (colorSelect.value === "Other" && customColorInput.value.trim() !== "") {
            colorSelect.value = customColorInput.value.trim();
        }
    });
</script>


 
